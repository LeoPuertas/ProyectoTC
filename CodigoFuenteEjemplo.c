int _x = 0; 
int _x = 2;       

double exec (double);
double exec2 (double);

int main () {
    double y = 1; z = 3.5;

    for (_x = 5; x < 10; _x = _x + 1)
        y = exec (y) + exec(z);

    if (y < 100) {
        y = y / 2; 
        z = y - 5.5;
    }
    else
        z = y + 5.5;
    if( y == 1){ 
        int c,d,f; 
        c = (( x + d) * f) / (7 * 20);
        c = 1;
    }
    return 0;
}


int _x2 = 2;
double exec (double valor, double valor2) {
    int i = 10;
    while ((i >= 5) && (valor > 3.4))
        valor -= _x - i;
    int y;
    return valor;
}
 
int _x3, _x4 = 5;

