int _x = 0;
int _x = 0;

double exec (double int);
double exec2 (double);
double exec3 (double);

double exec (double valor) {
    int i = 10;
    double a = 5
    while ((i >= 5) && (valor > 3.4))
        valor -= _x - i;
    return w;
}
   
   
double exec3 (double valor) {
    int i = 10, b;
    char a = 'j';
    i = 'j';
    b = a + i;
    while ((i >= 5) && (valor > 3.4))
        valor -= _x - i;
    return w;
}

double exec4 (double valor) {
    int i = 10;
    double a = 5;
    while ((i >= 5) && (valor > 3.4))
        valor -= _x - i;
    return a;

  
  
int main () {
    double y = 1, z = 3.5
    
    for _x = 5; _x < 10; _x = _x + 1)
        y = exec2(y) + exec(z + exec(z);

    if (y < 100 {
        y = y / 2
        z = y - 5.5;
    }
    else
        z = y + 5.5;

    return ;
}

     
